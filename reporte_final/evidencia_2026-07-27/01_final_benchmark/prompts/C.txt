You are working only in:
/home/alcazarjuanc/ai-stack/benchmarks/qwopus-np4/repos/agent-c-ml

Do not ask for confirmation.
Do not stop for approval.
Do not ask follow-up questions.
Make direct edits.
Do not read or modify files outside this repository.
Use python3 only.
Do not install packages.
Do not use the network.

Modify only:
- modeling.py
- test_modeling.py

Do not modify data.csv.

Use pandas and scikit-learn.

Implement exactly these public functions:

1. load_dataset(path, target_column)
- Read CSV with pandas.
- Return X, y.
- target_column must not remain inside X.
- Raise ValueError if target_column does not exist.

2. split_dataset(X, y, test_size=0.25, random_state=42)
- Use train_test_split.
- Use stratify=y.
- Return X_train, X_test, y_train, y_test.

3. build_model(random_state=42)
- Return a sklearn Pipeline.
- Step "scaler": StandardScaler.
- Step "model": LogisticRegression.
- LogisticRegression must use max_iter=1000.
- LogisticRegression must use the supplied random_state.

4. train_model(model, X_train, y_train)
- Fit the supplied model.
- Return the fitted model.

5. evaluate_model(model, X_test, y_test)

Return exactly:
{
    "accuracy": float,
    "balanced_accuracy": float,
    "f1": float
}

6. predict(model, X)
- Return predictions as a Python list of integers.

Do not leak the target into the feature matrix.
Do not add unrelated public functions.

Write meaningful pytest tests covering normal cases and edge cases.

TEST AND RECOVERY PROTOCOL:

1. After implementing the solution, run exactly once:
   python3 -m pytest -q

2. If it passes, continue to the final git diff.

3. If it fails, DO NOT immediately repeat the same pytest command.

4. Instead run:
   python3 -m pytest -x -vv || true

5. Read the complete failure carefully.
   Inspect the relevant implementation and test code.
   Identify the underlying cause before making another test attempt.

6. Make a targeted correction.

7. Then run:
   python3 -m pytest -q

8. If tests still fail, again DO NOT blindly repeat the failing command.
   Diagnose with:
   python3 -m pytest -x -vv || true
   Then inspect, fix, and retest.

9. Never execute the same failing Bash command repeatedly without changing code or diagnostic strategy.

10. Continue until all tests pass.

After all tests pass run:
git diff -- modeling.py test_modeling.py

Finish only after all tests pass.
